# Copy to .env and fill in your values

# Anthropic API key (optional - falls back to rule-based signals if not set)
ANTHROPIC_API_KEY=your_key_here

# Trading settings
DEFAULT_ACCOUNT_SIZE=10000
MAX_RISK_PER_TRADE_PCT=1.0
